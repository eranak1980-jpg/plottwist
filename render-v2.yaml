services:
  - type: web
    name: plottwist-v2-preview
    runtime: python
    branch: plottwist-2
    buildCommand: pip install -r requirements.txt && python test_engine.py
    startCommand: python app.py
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.7
